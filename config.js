module.exports = {

    // concurrency = downloads simultâneos durante a execução. 
    // Padrão 10
    concurrency: 4, 
    
    // pausa em ms (1000 = 1s) entre os requests. Tente um numero maior que zero (ex.: 200 ou 1000) se estiver tendo problemas com servidor destino. 
    // Padrão 0
    delay: 0

}